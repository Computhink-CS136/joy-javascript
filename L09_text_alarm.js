function setup() {
    createCanvas(600, 400)
    background("pink")

    textAlign(CENTER, BOTTOM)
    textColor("cyan");
    textSize(32);
    text("Bounce!", width/2, 200);  
}

function draw() {
    
}